type CityEntity = {
  name: string;
  canton: string;
  postalCode: string;
};

type CityResult = {
  name: string;
  canton: string;
  postalCode: string;
};

export type { CityEntity, CityResult };
